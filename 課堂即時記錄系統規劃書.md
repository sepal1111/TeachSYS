# **國小課堂即時記錄系統 (Teacher Classroom Management App)**

**需求與規格規劃書 (可攜式單機伺服器版)**

## **一、 系統定位與核心目標**

* **主要使用者：** 國小導師與科任教師（個人化管理工具，不對學生/家長開放）。  
* **使用情境：**  
  * **主機端：** 免安裝，放置於隨身碟中。插入 Windows 電腦後執行單一檔案 (.exe) 啟動系統伺服器，可搭配教室大螢幕顯示。  
  * **操作端：** 教師透過手機或平板，連接同一個教室 Wi-Fi 網路，瀏覽器開啟介面進行即時操作（走動式管理）。  
* **核心價值：** 直覺、快速、減少點擊次數，整合「出缺席、加減分、質性紀錄、彈性分組與成績匯出」，降低班級經營負擔，且資料具備最高隱私性與便攜性。

## **二、 核心功能向度與規格**

### **向度一：課程與學生管理 (對象維度)**

1. **身份與課程架構：**  
   * 支援「導師」與「科任」模式，皆以「課程 (Course)」為管理單位（如：導師的國語、數學；科任的自然301、自然302）。  
   * 首頁提供「我的課程列表」，點擊按鈕進入特定課程。  
2. **彈性分組系統：**  
   * 小組名單依附於「課程」，各課程可有獨立分組。  
   * **分組方式：** 教師設定組數/每組人數後，系統支援：  
     * 完全隨機分組  
     * **性別平均隨機分組**（需於匯入名單時包含性別欄位）  
     * 教師手動指定分組  
   * **微調機制：** 任何自動分組完成後，皆支援「手動拖曳 (Drag & Drop)」調整學生組別。  
3. **點名與出缺席紀錄：**  
   * 進入課程後的首要動作或獨立功能頁籤。  
   * 標記缺席、遲到、病假等狀態。  
   * 狀態需能被系統記錄並支援後續匯出。  
   * *防干擾設計：* 標記缺席的學生，在後續的加減分介面中應有視覺提示（如：頭像反灰），避免誤點。

### **向度二：評分與紀錄機制 (操作維度)**

1. **量化加減分 (預設選項制)：**  
   * **操作流程：** 選擇方向（正向/負向） ➡️ 點選預設項目 ➡️ 套用至「個人」或「小組」。  
   * 預設項目與對應分數由教師於後台事前設定。  
2. **質性文字紀錄：**  
   * 提供獨立的「📝 特殊表現紀錄」按鈕，與加減分脫鉤。  
   * 針對單一學生記錄行為表現、情緒狀態等備忘錄。  
3. **防呆機制 (Undo 復原)：**  
   * 在進行加減分或點名操作後，畫面上短暫出現「復原 (Undo)」按鈕（如：顯示 3-5 秒的 Toast 提示），允許快速撤銷上一步誤觸的操作。

### **向度三：數據呈現與報表 (回饋與應用維度)**

1. **課堂即時視覺化 (儀表板)：**  
   * 學生頭像/名牌旁預設顯示「當天累積分數」。  
   * 具備切換按鈕，可快速切換顯示：「當天」、「指定日期區間總計」、「學期總計」。  
2. **即時排行榜：**  
   * 系統自動生成排行榜，可切換「當日排行」或「指定日期區間排行」，供教師課堂上運用（如：獎勵前三名）。  
3. **資料匯出 (Excel)：**  
   * 支援選擇「指定日期區間」。  
   * 匯出內容包含：出缺席統計、加減分總計（量化）、文字紀錄明細（質性）。

### **向度四：系統架構、佈局與設定 (IT 與 UI/UX 維度)**

1. **技術架構與部署 (便攜式區域網路伺服器)：**  
   * **系統封裝：** 系統所有程式碼與環境打包成單一 Windows .exe 執行檔，放置於隨身碟。  
   * **資料庫：** 採用 SQLite 單機資料庫，資料庫檔案 (.db) 直接儲存於隨身碟的同一資料夾中。  
   * **連線機制：** 電腦端執行後開啟區域網頁伺服器，介面提供「本機網址」與「QR Code」。教師手機/平板連上教室 Wi-Fi 後，掃描即可操作。  
   * **備份機制：** 教師只需透過 Windows 檔案總管，手動複製隨身碟內的 SQLite 資料庫檔案或整個資料夾即可完成備份。  
2. **裝置適應性 (RWD 響應式網頁設計)：**  
   * **手機 (操作端 \- Mobile-first)：** 優先考量單手直立操作。核心按鈕置於中下方，學生清單以網格/列表呈現，支援上下滑動。  
   * **平板 (操作端)：** 運用較大螢幕空間，將選單與學生列表分置左右兩欄，減少畫面切換。  
   * **大螢幕/電腦 (展示端)：** 優化排行榜與全班計分板的視覺呈現，適合投影給全班觀看。  
3. **後台管理 (可透過電腦端或平板操作)：**  
   * 管理學生名單（批次匯入、設定性別）。  
   * 自訂加減分項目名稱與分數額度。  
   * 學期資料的結算與封存。

## **五、 開發與維護規範 (給 AI 開發者的核心指令)**

> **⚠️ \[SYSTEM PROMPT / 開發者必讀\] ⚠️**

> 任何接手此專案開發的 AI (包含 Claude)，在進行任何程式碼撰寫、修改或架構調整時，**必須嚴格遵守以下紀錄規範**，絕不可遺漏：

1. **建立專屬的 Changelog 檔案：** 在專案根目錄必須建立並維護一個名為 CHANGELOG.md 的檔案。  
2. **每次修改強制寫入 Log：** 在每一次對話回覆中，只要有提供修改後的程式碼，就必須在回覆的最後附上要新增至 CHANGELOG.md 的更新紀錄，最新修改的記錄要再最上方。  
3. **Log 格式規範：** 需包含「日期時間」、「修改模組/檔案」、「修改類別 (新增/修正/優化/移除)」、「具體修改內容說明」。